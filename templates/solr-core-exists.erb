#!/bin/bash

# Test if a solr core exists

if [ "$1" == "" ]; then
    echo 'specify a name'
    exit 1
fi

curl -q -f "http://localhost:8080/solr/admin/cores?action=STATUS&core=$1" | grep instanceDir

exit $?