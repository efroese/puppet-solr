#!/bin/bash

# Test if a solr core exists

if [ "$1" == "" ]; then
    echo 'specify a name'
    exit 1
fi

if [ -d "<%= scope.lookupvar('solr::params::data') %>/$1" ]; then
    exit 0
else
    exit 1
fi
